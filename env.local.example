# Backend Configuration
# Choose 'tauri' for native desktop app or 'express' for web API
VITE_BACKEND_TYPE=tauri

# Express API URL (only used when VITE_BACKEND_TYPE=express)
VITE_API_URL=http://localhost:3000/api

